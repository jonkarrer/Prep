version: '3.8'

services:
  web_server:
    build:
      context: .
      dockerfile: Dockerfile.rust
    ports:
      - "8000:8000"
    env_file:
      - .env

  # nginx:
  #   build:
  #     context: .
  #     dockerfile: Dockerfile.nginx
  #   ports:
  #     - "80:80"
  #   depends_on:
  #     - rust_web_app
